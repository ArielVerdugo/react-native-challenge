import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: 'white',
  },
  textTitle: {
    padding: 10,
    fontSize: 20,
    fontWeight: '500',
  },
  imageHeader: {
    height: 300,
    width: '100%',
  },
});
