import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  background: {
    flex: 1,
  },
  blur: {
    position: 'absolute',
    width: '100%',
    height: '100%',
  },
});

export default styles;
